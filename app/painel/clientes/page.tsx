import PainelClientes from '../../components/PainelClientes';

export const metadata = {
  title: 'Clientes — Painel Nefertari',
  robots: 'noindex, nofollow',
};

export default function ClientesPage() {
  return <PainelClientes />;
}
