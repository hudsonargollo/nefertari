import Painel from '../components/Painel';

export const metadata = {
  title: 'Painel da Cozinha — Nefertari',
  robots: 'noindex, nofollow',
};

export default function PainelPage() {
  return <Painel />;
}
